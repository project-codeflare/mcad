---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.12.0
  name: appwrappers.mcad.my.domain
spec:
  group: mcad.my.domain
  names:
    kind: AppWrapper
    listKind: AppWrapperList
    plural: appwrappers
    singular: appwrapper
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: STATUS
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AppWrapper is the Schema for the appwrappers API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: AppWrapperSpec defines the desired state of AppWrapper
            properties:
              gpus:
                description: gpus needed
                format: int32
                type: integer
              pods:
                description: expected pod count
                format: int32
                type: integer
              priority:
                description: priority
                format: int32
                type: integer
              resources:
                description: resources
                items:
                  type: object
                  x-kubernetes-preserve-unknown-fields: true
                type: array
            type: object
          status:
            description: AppWrapperStatus defines the observed state of AppWrapper
            properties:
              lastdispatchtime:
                description: when dispatched
                format: date-time
                type: string
              phase:
                description: 'phase: Queued, Dispatching, Running, Completed, Failed,
                  Terminating'
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
